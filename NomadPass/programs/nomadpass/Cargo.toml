[package]
name = "nomadpass"
version = "0.1.0"
description = "NomadPass Solana credential registry"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "nomadpass"

[features]
no-entrypoint = []
no-idl = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
